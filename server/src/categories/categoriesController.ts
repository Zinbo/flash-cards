const sayHello = () => {
  const hello: string = 'say hello from the category controller'
  return hello
}

export default sayHello

// Will contain a controller class which will call the category service
